# react-seo-bridge SEO Audit Report

**Project:** `{{ r.project_name }}`
**Analysed:** {{ r.analysed_at }}
**rsb version:** {{ r.rsb_version }}

---

## SEO Score: {{ r.seo_score }}/100

{% if r.seo_score >= 80 %}
**Good** - Minor improvements recommended.
{% elif r.seo_score >= 50 %}
**Needs work** - Several SEO issues detected.
{% else %}
**Critical issues** - This site is likely poorly indexed by search engines.
{% endif %}

| Metric | Value |
|--------|-------|
| Rendering type | `{{ r.bundle.rendering_type.value }}` |
| Framework | `{{ r.bundle.framework }}` |
| React Router version | `{{ r.bundle.react_router_version or 'not detected' }}` |
| Total routes found | {{ r.total_routes }} |
| Dynamic routes | {{ r.dynamic_routes }} |
| Lazy-loaded routes | {{ r.lazy_routes }} |
| Files with client-side metadata | {{ r.routes_with_client_metadata }} |

---

## Issues Summary

| Severity | Count |
|----------|-------|
| Critical | {{ r.critical_count }} |
| High | {{ r.high_count }} |
| Medium | {{ r.medium_count }} |
| Low | {{ r.low_count }} |

---

## Issues Detail

{% for issue in r.issues %}
### {{ loop.index }}. {{ issue.title }}

**Severity:** {{ issue.severity.value.upper() }}
**Category:** {{ issue.category }}

{{ issue.description }}

**Recommendation:** {{ issue.recommendation }}

{% if issue.affected_files %}
**Affected files:**
{% for f in issue.affected_files %}
- `{{ f }}`
{% endfor %}
{% endif %}

{% if issue.affected_routes %}
**Affected routes:** {% for route in issue.affected_routes %}`{{ route }}`{% if not loop.last %}, {% endif %}{% endfor %}
{% endif %}

{% if issue.docs_url %}
**Reference:** {{ issue.docs_url }}
{% endif %}

---
{% endfor %}

## Discovered Routes

| Path | Dynamic | Lazy | Data Fetching | File |
|------|---------|------|---------------|------|
{% for route in r.routes %}
| `{{ route.path }}` | {{ 'Y' if route.is_dynamic else '-' }} | {{ 'Y' if route.is_lazy else '-' }} | {{ route.data_fetch_pattern.value }} | `{{ (route.component_file or 'unknown').split('/')[-1] }}` |
{% endfor %}

---

## Core Web Vitals Estimates

| Signal | Risk | Notes |
|--------|------|-------|
| LCP (Largest Contentful Paint) | {{ r.cwv.lcp_risk.value.upper() }} | Avg {{ r.cwv.estimated_js_imports_per_route }} imports/route |
| CLS (Cumulative Layout Shift) | {{ r.cwv.cls_risk.value.upper() }} | {{ r.cwv.images_without_dimensions }} images without dimensions |
| INP/FID | {{ r.cwv.fid_risk.value.upper() }} | {{ r.cwv.lazy_loaded_routes }} lazy routes |

{% if r.cwv.notes %}
### CWV Notes
{% for note in r.cwv.notes %}
- {{ note }}
{% endfor %}
{% endif %}

---

## Bundle Analysis

| Property | Value |
|----------|-------|
| Framework | {{ r.bundle.framework }} |
| React version | {{ r.bundle.react_version or 'unknown' }} |
| React Router | v{{ r.bundle.react_router_version or '?' }} |
| SSR setup detected | {{ 'Yes' if r.bundle.has_ssr_setup else 'No' }} |
| SSG setup detected | {{ 'Yes' if r.bundle.has_ssg_setup else 'No' }} |
| react-helmet | {{ 'Yes' if r.bundle.has_helmet else 'No' }} |
| react-helmet-async | {{ 'Yes' if r.bundle.has_helmet_async else 'No' }} |
| Existing prerender tool | {{ 'Yes' if r.bundle.has_existing_prerender else 'No' }} |
| Total dependencies | {{ r.bundle.total_dependencies }} |

---

## Next Steps

{% if r.bundle.rendering_type.value == 'csr' %}
1. **Immediate (days):** Add `sitemap.xml` and `robots.txt` to your `/public` folder.
2. **Short-term (weeks):** Add Open Graph and canonical tags via react-helmet-async to all page components.
3. **Medium-term (months):** Migrate to Next.js (App Router + Server Components) for native SSR/SSG.
   Use `rsb scaffold` (coming in v0.2) to generate a file-by-file migration plan.
4. **Alternative:** Deploy react-seo-bridge Mode A (dynamic rendering) as a bridge solution
   while planning the Next.js migration.
{% else %}
1. Address the issues listed above in severity order.
2. Re-run `rsb audit` after each fix to track improvement.
{% endif %}

---

*Generated by [react-seo-bridge](https://github.com/your-org/react-seo-bridge)*
*This report is a static analysis estimate. Use Google Search Console for live crawl data.*
